# Codecov configuration for NOAA-GFDL/fremor
# Reference: https://docs.codecov.com/docs/codecovyml-reference

coverage:
  status:
    project:
      default:
        target: 96%          # project is not allowed to fall below this overall
        if_ci_failed: error

    patch:
      default:
        target: 80%          # Minimum coverage for new code
        if_ci_failed: error

comment:
  layout: "header, diff, flags, components, files, footer"
  behavior: default
  require_changes: false
